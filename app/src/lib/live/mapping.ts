// Moved to the framework-free core package as core/wire.ts. Re-export shim: keeps
// `../live/mapping` imports (app + extension) working unchanged. Source of truth: core/wire.ts.
export * from "../../../../core/wire";
